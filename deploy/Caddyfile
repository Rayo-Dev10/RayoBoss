# Reemplazar por el subdominio institucional real.
radio.example.edu {
    encode zstd gzip
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
    }
    reverse_proxy 127.0.0.1:3000
}
